import type { IncomingMessage, ServerResponse } from 'node:http';
const MODEL='nvidia/nemotron-3-ultra-550b-a55b:free';
export default async function handler(req:IncomingMessage&{body?:{messages?:unknown}},res:ServerResponse){if(req.method!=='POST'){res.statusCode=405;res.end(JSON.stringify({error:'Method not allowed'}));return}const key=process.env.OPENROUTER_API_KEY;if(!key){res.statusCode=500;res.end(JSON.stringify({error:'OPENROUTER_API_KEY is not configured.'}));return}try{const r=await fetch('https://openrouter.ai/api/v1/chat/completions',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json','X-Title':'MAAR AI'},body:JSON.stringify({model:MODEL,messages:req.body?.messages??[],temperature:.3})});const d=await r.json() as {choices?:Array<{message?:{content?:string}}>;error?:{message?:string}};res.statusCode=r.status;res.setHeader('Content-Type','application/json');res.end(JSON.stringify(r.ok?{content:d.choices?.[0]?.message?.content??''}:{error:d.error?.message??'OpenRouter request failed.'}));}catch(e){res.statusCode=500;res.end(JSON.stringify({error:e instanceof Error?e.message:'Unexpected server error.'}))}}
