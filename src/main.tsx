import { StrictMode, useState } from 'react';
import { createRoot } from 'react-dom/client';
import './styles.css';
const MODEL='nvidia/nemotron-3-ultra-550b-a55b:free';
type Message={role:'user'|'assistant';content:string};
function App(){const[messages,setMessages]=useState<Message[]>([]),[input,setInput]=useState(''),[loading,setLoading]=useState(false);async function send(){const text=input.trim();if(!text||loading)return;const next=[...messages,{role:'user' as const,content:text}];setMessages(next);setInput('');setLoading(true);try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({messages:next})});const d=await r.json();if(!r.ok)throw new Error(d.error||'MAAR AI request failed.');setMessages([...next,{role:'assistant',content:d.content||'No response.'}]);}catch(e){setMessages([...next,{role:'assistant',content:`Sorry — ${e instanceof Error?e.message:'Something went wrong.'}`}]);}finally{setLoading(false)}}return <div className="app"><header><div className="logo">M</div><div><h1>MAAR AI</h1><p>Simple AI for students</p></div><span className="status">● Online</span></header><main><section className="hero"><div className="orb">M</div><h2>What can I help you learn?</h2><p>Ask a question and MAAR AI will help you understand it.</p></section><div className="messages">{messages.map((m,i)=><div key={i} className={`message ${m.role}`}><div className="bubble">{m.content}</div></div>)}{loading&&<div className="message assistant"><div className="bubble">MAAR AI is thinking…</div></div>}</div><div className="composer"><textarea value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();void send()}}} placeholder="Ask MAAR AI anything…" rows={1}/><button onClick={()=>void send()} disabled={loading||!input.trim()}>↑</button></div><p className="model">Powered by OpenRouter · {MODEL}</p></main></div>}
createRoot(document.getElementById('root')!).render(<StrictMode><App/></StrictMode>);
